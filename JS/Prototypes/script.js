/* Class

        class MusicPlayer
        {
            constructor(format)
            {
                this.format = format;
                console.log(this.format);
            }
        }

        let player = new MusicPlayer("MP3"); 
*/

